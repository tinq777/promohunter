{
  "name": "promohunter-worker",
  "version": "1.0.0",
  "description": "PromoHunter Cloudflare Worker — server-side coupon scraping",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
